<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/pc192"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/pc192_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CaritasRig"
        tools:targetApi="31">

        <activity
            android:name=".activity.CheckActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activity.LoginActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.CaritasRig">
        </activity>
        <activity android:name=".activity.HomeActivity" />
        <activity android:name=".activity.SignUpActivity" />
        <activity android:name=".activity.RegisterActivity" />
        <activity android:name="com.canhub.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat"/>
        <activity android:name=".activity.AboutUsActivity" />
        <activity android:name=".activity.BuildActivity"/>
        <activity android:name=".activity.build.CasingActivity"/>
        <activity android:name=".activity.build.CpuActivity"/>
        <activity android:name=".activity.build.InternalHardDriveActivity"/>
        <activity android:name=".activity.build.MemoryActivity"/>
        <activity android:name=".activity.build.MotherboardActivity"/>
        <activity android:name=".activity.build.PowerSupplyActivity"/>
        <activity android:name=".activity.build.VideoCardActivity"/>
    </application>

</manifest>